name: E2E Tests

# Run E2E tests before merging to main
on:
  pull_request:
    branches:
      - main
  workflow_dispatch: # Allow manual trigger

jobs:
  e2e-tests:
    name: Run E2E Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    # The results step posts a PR comment; the default token is read-only,
    # which previously failed with "Resource not accessible by integration".
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      # `npm run test:e2e` sets TEST_ENV=local, and playwright.config.ts loads
      # `.env.test.${TEST_ENV}` — i.e. `.env.test.local`. Writing plain
      # `.env.test` meant the values were never loaded and the config threw
      # "Missing required environment variables" before any test ran.
      - name: Create .env.test.local file
        run: |
          echo "VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }}" >> .env.test.local
          echo "VITE_SUPABASE_ANON_KEY=${{ secrets.VITE_SUPABASE_ANON_KEY }}" >> .env.test.local
          echo "TEST_USER_EMAIL=${{ secrets.TEST_USER_EMAIL }}" >> .env.test.local
          echo "TEST_USER_PASSWORD=${{ secrets.TEST_USER_PASSWORD }}" >> .env.test.local

      - name: Run E2E tests
        run: npm run test:e2e
        env:
          CI: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

      - name: Upload test videos on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-videos
          path: test-results/
          retention-days: 7

      - name: Comment PR with test results
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let comment = '## E2E Test Results\n\n';

            try {
              const results = JSON.parse(fs.readFileSync('playwright-report/results.json', 'utf8'));
              const passed = results.suites.reduce((acc, suite) => {
                return acc + suite.specs.filter(spec => spec.ok).length;
              }, 0);
              const failed = results.suites.reduce((acc, suite) => {
                return acc + suite.specs.filter(spec => !spec.ok).length;
              }, 0);
              const total = passed + failed;

              comment += `✅ Passed: ${passed}\n`;
              comment += `❌ Failed: ${failed}\n`;
              comment += `📊 Total: ${total}\n\n`;

              if (failed > 0) {
                comment += '### Failed Tests\n';
                results.suites.forEach(suite => {
                  suite.specs.filter(spec => !spec.ok).forEach(spec => {
                    comment += `- ${spec.title}\n`;
                  });
                });
              }
            } catch (error) {
              comment += '⚠️ Could not parse test results\n';
            }

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });
